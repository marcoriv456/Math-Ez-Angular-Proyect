import { InputEditableElementDirective } from './input-editable-element.directive';

describe('InputEditableElementDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEditableElementDirective();
    expect(directive).toBeTruthy();
  });
});
