import { InputTermDirective } from './input-term.directive';

describe('InputTermDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTermDirective();
    expect(directive).toBeTruthy();
  });
});
