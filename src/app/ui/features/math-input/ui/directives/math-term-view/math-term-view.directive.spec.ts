import { MathTermViewDirective } from './math-term-view.directive';

describe('MathTermViewDirective', () => {
  it('should create an instance', () => {
    const directive = new MathTermViewDirective();
    expect(directive).toBeTruthy();
  });
});
