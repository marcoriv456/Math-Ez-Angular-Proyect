import {RellaxDirective} from './rellax.directive';

describe('RellaxDirective', () => {
  it('should create an instance', () => {
    const directive = new RellaxDirective();
    expect(directive).toBeTruthy();
  });
});
